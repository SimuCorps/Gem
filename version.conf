# Gem Programming Language Version Configuration
# This is the single source of truth for version information

VERSION_MAJOR=1
VERSION_MINOR=2
VERSION_PATCH=0
VERSION_SUFFIX=

# Derived version strings (automatically calculated)
# VERSION_STRING will be "1.2.0" or "1.2.0-beta" if suffix is set
# VERSION_DISPLAY will be "v1.2.0" for display purposes 